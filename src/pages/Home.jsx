import { Download, ImageUp, Network, WandSparkles } from 'lucide-react';
import Hero from '../components/Hero';
import FeatureCard from '../components/FeatureCard';
import PredictionCard from '../components/PredictionCard';
const features=[{icon:ImageUp,title:'Upload Satellite Image',text:'Bring in high-resolution TIFF or GeoTIFF imagery with a frictionless upload flow.'},{icon:WandSparkles,title:'Deep Learning Prediction',text:'A trained U-Net analyzes every pixel to identify traversable road corridors.'},{icon:Network,title:'Road Mask Generation',text:'Receive a crisp segmentation mask and transparent visual overlay in seconds.'},{icon:Download,title:'Download Results',text:'Export visual outputs for reporting, GIS workflows, or model validation.'}];
export default function Home({onNavigate}){return <div className="space-y-20"><Hero onNavigate={onNavigate}/><section><div className="mb-8 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-orange-300">Intelligence workflow</p><h2 className="mt-2 text-3xl font-bold">From imagery to insight</h2></div><button onClick={()=>onNavigate('predict')} className="hidden text-sm text-violet-200 sm:block">Open prediction studio →</button></div><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{features.map((feature,index)=><FeatureCard key={feature.title} {...feature} index={index}/>)}</div></section><section id="predict"><PredictionCard/></section></div>}
