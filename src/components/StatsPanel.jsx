import { BarChart3, Cpu, Image, Map, Ruler, Timer, Trophy } from 'lucide-react';
import { motion } from 'framer-motion';
const definitions = [[Map, 'Road Pixels', 'roadPixels'], [BarChart3, 'Road Coverage', 'coverage'], [Ruler, 'Estimated Road Length', 'roadLength'], [Image, 'Image Size', 'size'], [Timer, 'Prediction Time', 'time'], [Cpu, 'Model Name', 'model'], [Trophy, 'Confidence', 'confidence']];
export default function StatsPanel({ stats }) { return <section className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{definitions.map(([Icon, label, key], index) => <motion.article key={key} initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:index*.05}} whileHover={{y:-4}} className="glass rounded-2xl p-4"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-300/10 text-violet-200"><Icon size={19}/></span><div><p className="text-xs text-slate-400">{label}</p><p className="mt-1 text-sm font-bold text-white">{stats[key]}</p>{key === 'roadPixels' && <p className="text-[10px] text-slate-500">Background: {stats.backgroundPixels}</p>}</div></div></motion.article>)}</section>; }
