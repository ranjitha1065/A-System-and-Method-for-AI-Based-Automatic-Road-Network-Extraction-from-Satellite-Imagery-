import { Code2, Moon, Sparkles } from 'lucide-react';
const links = [['home', 'Home'], ['predict', 'Predict'], ['disaster', 'Disaster Analysis'], ['about', 'About']];
export default function Navbar({ active, onNavigate }) { return <header className="fixed inset-x-0 top-0 z-40"><nav className="mx-auto mt-4 flex max-w-7xl items-center justify-between rounded-2xl border border-white/10 bg-slate-950/45 px-5 py-3 backdrop-blur-xl lg:px-7"><button onClick={() => onNavigate('home')} className="flex items-center gap-2 font-[Manrope] text-lg font-extrabold"><span className="grid h-9 w-9 place-items-center rounded-xl gradient-button"><Sparkles size={18}/></span>Road<span className="gradient-text">AI</span></button><div className="hidden items-center gap-7 md:flex">{links.map(([key,label]) => <button key={key} onClick={() => onNavigate(key)} className={`text-sm transition ${active === key ? 'text-white' : 'text-slate-400 hover:text-white'}`}>{label}</button>)}<a href="https://github.com" target="_blank" rel="noreferrer" className="text-slate-400 hover:text-white"><Code2 size={19}/></a><button className="rounded-full border border-white/10 p-2 text-slate-300"><Moon size={16}/></button></div></nav></header>; }
